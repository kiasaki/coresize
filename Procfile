web: coresize \
  -hash $HASH \
  -port $PORT \
  -folder-name files/ \
  -pull-from $PULL_FROM \
  -pull-from-url $PULL_FROM_URL \
  -aws-client-key $AWS_CLIENT_KEY \
  -aws-secret-key $AWS_SECRET_KEY

